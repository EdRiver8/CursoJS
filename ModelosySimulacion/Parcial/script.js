//definicion de variables
//cantidad personas a contrar y probabilidad
let empresaAz = {
  0: 0.1,
  1: 0.6,
  2: 0.3,
};

let empresaBy = {
  0: 0.4,
  1: 0.2,
  2: 0.4,
};

let empresaCw = {
  0: 0.15,
  1: 0.3,
  2: 0.4,
  3: 0.1,
  4: 0.05,
};

let java = 0.5;
let scala = 0.3;
let python = 0.2;

let experto = 5000;
let costoExpertoJava = 2100;
let costoExpertoScala = 1800;
let costoExpertoPython = 1900;

//simulacion a 20 años, mensualmente 12 * 20 = 240
for (let index = 0; index < 240; index++) {
  //generacion variable aleatoria, seleccion empresa
  let rndAz = Math.random();
  let rndBy = Math.random();
  let rndCw = Math.random();
  switch (key) {
    case value:
      break;

    default:
      break;
  }
}
