export const messageTXT = "Bienvenido"; //no mutable
//mutable por ser un objeto
export const config = {
  user: "Prueba",
  estado: false,
};

export function show(text) {
  return `Prueba ${text}`;
}

const API_KEY = "545454ASD";

export default API_KEY;
